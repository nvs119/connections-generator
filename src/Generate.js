import "./Generate.css";

export default function GenerateButton() {
    return (
        <div>
            <button className="generateButton">
                <p><b>generate</b></p> 
            </button>
        </div>
    )
}